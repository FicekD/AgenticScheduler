# Work Plan

Numbered points, worked top to bottom. A point is done when
`.agentic-scheduler/reports/point-N.md` exists.

1. Scaffold a tiny Node CLI `greet` that prints a greeting for a `--name` argument, with a
   README and one test. Branch from main.
2. Add a `--lang` flag supporting `en`, `cs`, and `de` greetings, with tests for each.
3. Add input validation and a `--json` output mode, with tests.
